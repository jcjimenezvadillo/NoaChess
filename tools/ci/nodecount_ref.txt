140411

# Regenerated 2026-09-22 against the 5.9.15 tree (GoodCaptureSlack,
# NmpEvalR, TtNoPvCutoff, FutilityFailSoft, ImprovingAboveBeta and
# CheckExemptFutility on by default; 5.9.14 gave 141122, 5.9.13 141262,
# 5.9.12 126949), with the published Release binary and UseNNUE=false forced by the
# check. The previous number (258577, generated 2026-08-30 against 5.3.0) had
# not been regenerated since, so the check had failed on every release after
# 5.3.0; v5.9.11 gives 133561. The number is tied to the SEARCH plus the
# classical evaluator only, never to a network (the embedded net is
# gitignored, so a CI checkout builds a network-free binary; the check forces
# the classical path so local and CI totals agree). When the search or the
# classical evaluator changes, regenerate by hand with
# tools/ci/check_nodecount.ps1 and note in the commit which source produced
# it. Never automatically.
