using System.Windows;

namespace NoaChess.GUI.Wpf.Views;

// Reference card for the gestures the board understands. Purely declarative:
// the whole content lives in the XAML.
public partial class HelpDialog : Window
{
    public HelpDialog() => InitializeComponent();
}
